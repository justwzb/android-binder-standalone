FIND_PACKAGE(Kernel REQUIRED)
INCLUDE_DIRECTORIES(${Kernel_INCLUDE_DIR})

set(SERVICE_MANAGER_SRC
   ${CMAKE_CURRENT_SOURCE_DIR}/service_manager.c
   ${CMAKE_CURRENT_SOURCE_DIR}/binder.c
)
ADD_EXECUTABLE(servicemanager ${SERVICE_MANAGER_SRC})